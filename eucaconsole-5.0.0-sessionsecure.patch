Index: eucaconsole/conf/console.ini
===================================================================
--- eucaconsole.orig/conf/console.ini
+++ eucaconsole/conf/console.ini
@@ -91,7 +91,7 @@ session.key = eucaconsole_session
 # session.keyini is the location of the session key file, which is autogenerated during console start if missing
 session.keyini = /etc/eucaconsole/session-keys.ini
 # session.secure = true implies an HTTPS reverse proxy (nginx or load balancer) is configured
-session.secure = false
+session.secure = true
 # Idle timeout (1800 sec = 30 min)
 session.timeout = 1800
 # Absolute timeout (defaults to 43200 sec, or 12 hours)
